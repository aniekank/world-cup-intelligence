# Bug Log — World Cup Intelligence

A living tracker of bugs, their root causes, and fixes. Keep it next to the code so the
record travels with the repo. Commit hashes let anyone see the exact change.

**Last updated:** 2026-07-05 (WC-082)

---

## How to log a bug

Copy this block into **Open bugs** when you find something during testing:

```
### WC-0XX — <short title>
- **Area:** <page / module, e.g. /matches, search, store, narratives>
- **Severity:** blocker | high | medium | low | cosmetic
- **Steps:** <exact steps to reproduce>
- **Expected:** <what should happen>
- **Actual:** <what happens, with the exact error/text if any>
- **Notes:** <screenshots, console error, which data source (live / simulation), time zone>
- **Status:** Open
```

When testing, the single most useful thing to capture is the **exact on-screen text or console
error** and **which data source** was active (live-2026 vs simulation) — most bugs here behave
differently between the two.

**Status legend:** 🔴 Open · 🟡 Investigating · 🟢 Resolved · ⚪ Won't fix / by design

---

## Open bugs

### WC-077 — Live match (Canada v Morocco) disappears from live view at halftime
- **Area:** live view (home "Live & Upcoming" / /matches "Live now" / ticker), data source unknown
- **Severity:** high
- **Steps:** watch a live knockout match; at halftime it vanishes from the live surfaces
- **Expected:** a match at HT stays in the live view labelled "HT"
- **Actual:** the match disappears from the live view at halftime
- **Investigation:** traced the full pipeline — `mapStatus('HT')→HALFTIME` (apiFootball), the refresh/merge, `shouldForceFinish` (returns false at HT and can't fire inside the 210-min window regardless), the store selectors (`getLiveMatches`), `matchesView`, `liveStatus`, the refresh loop, and `MatchCard`/`LiveTicker` — **all correctly treat HALFTIME as live.** A hermetic repro (synthetic HT knockout match kicked off 50 min ago) shows it stays in `getLiveMatches`, `matchesView`, and is NOT force-finished. So this is **not a status-filter bug in the code.** Production check (quota-free reads of `/api/live-status` + `/api/matches`): the site is healthy on live data (`source: API-Football (live)`, `live-2026`, not loading/cached) — so it is NOT stuck on a sim/cache fallback. That leaves **feed jitter** as the likely mechanism: one refresh tick returned the in-play fixture as `NS`/SCHEDULED, and the merge trusted it unconditionally, flipping the match to SCHEDULED → out of every live surface until the next good tick.
- **Fix (hardening):** `isSpuriousRevert()` in `loadTournament.ts` — the merge now ignores a single SCHEDULED reading for a match already LIVE/HALFTIME within its play window (a started match can't un-start). Plus a quota-free diagnostic `/api/live-status?debug=1` (`liveDebug()` in `queries.ts`) dumping each in-play match's status/minute/kickoff-age/visibility + cache/backoff flags. Minor related find (not the cause, not yet fixed): `MiniMatchRow` (team/group pages) treats only `LIVE`, not `HALFTIME`, as live.
- **Status:** 🟡 Mitigated — jitter guard + diagnostic shipped; confirm with `?debug=1` at the next live game (root-cause tick not captured live)

_New items go below using the template above._

---

## Resolved bugs

| ID | Bug | Area | Root cause | Fix |
|----|-----|------|-----------|-----|
| WC-082 | Title odds didn't sum to 100% once the knockouts began — on Clash of the Civilizations the combined confederation title probabilities added up to well under 100% (and likewise across the field / Predictions), which is impossible (exactly one team wins). | analytics/knockoutResults (forecast reconcile) | `reconcileForecastsWithResults` correctly **zeroes** eliminated / non-qualified teams' `winTitle` when the bracket is known, but never **redistributed** that probability mass. The survivors kept their original Monte-Carlo odds (which summed to ~1 *including* the now-eliminated teams), so stripping the eliminated ones out left the total short by exactly the share they'd held. | After zeroing, **renormalize the survivors so `winTitle` sums to exactly 1** — the correct conditional given who's still in it — gated on `bracketKnown` (only once the group stage is complete; before that nothing has been zeroed, so there's nothing to redistribute). Fixes the totals everywhere `winTitle` is shown (Civilizations, Predictions, storylines). Reach ladder (reachR16…Final) carries the same partial-reconciliation artifact — noted; the full fix is the deferred real-bracket re-sim. Hermetic test: after two R32 results, survivors' odds renormalize 0.25→0.50 and the field sums to 1; the WC-038 "don't touch a still-settling field" test still passes. 118 tests, tsc + build green. · knockoutResults.ts |
| WC-081 | Proactive phase-lag sweep across the remaining narrative surfaces after WC-080: **AI Insights** and **Discoveries** still presented players/teams from knocked-out sides as live ("breakout star", "the tournament's meanest defense", a "form mover", an "under-the-radar difference-maker") with no fate; and "Squads to Watch" could pick an eliminated team as "Firepower"/"The Fortress" off static offense/defense ratings. | ai/narratives · server/discoveries | Same root as WC-080 — these are ranked by **cumulative or static** metrics (goals, ratings, momentum) that don't reset on elimination, with forward-looking copy. | Exposed a reusable `teamFateClause(teamId)` and appended it to the **breakout**, **golden-boot** and **wall** insight bodies and the **discoveries** under-rated-player blurb; **excluded eliminated sides** from the form-mover insight and from the offense/defense/momentum/delta pools that feed Squads-to-Watch (champions kept). Leaderboard boards (Defense, Golden Glove) left as-is — they're cumulative records, not live-framing. Verified: the golden-boot projection was already sound (reconciled reach probs → 0 caps eliminated players at current goals, WC-033). 117 tests, tsc + build green. · narratives.ts · discoveries.ts |
| WC-080 | Storylines ("Ones to Watch") presented players from **knocked-out teams as live stories** — a group-stage top scorer whose side had been eliminated still read "catch them while they're flying," with no indication their tournament was over. | ai/narratives · storylines page | Player storylines are ranked by **cumulative** output (goals, assists, form), which doesn't reset when a team is eliminated — so a group-stage hero keeps surfacing — while the archetype blurbs are written forward-looking. Nothing reflected the team's actual fate. | Added a penalty-aware `teamFate(teamId)` (out + the round they exited / runners-up / champions; `undefined` while still alive or pre-knockouts). Every player and squad storyline now carries a `fate`, rendered as a **badge** (red "Out · R16", "Runners-up", gold "🏆 Champions") and appended as an honest clause to the blurb ("…But that run is over — eliminated in the round of 16."). Hermetic test covers exit-round / runner-up / champion labelling. 117 tests, tsc + build green. · narratives.ts · storylines/page.tsx |
| WC-079 | The Analytics page showed **no data for xG or finishing** — "Total xG 0.0", an "Avg xG/shot 0.00", a Finishing scatter with every scorer collapsed onto the xG=0 line, and empty xG Leaders. Also two statistical-soundness bugs. | app/analytics · server/analytics | The whole page was built on **per-player xG** (`p.stats.xG`), which the live feed doesn't carry and the frozen SportMonks capture never captured — it's 0 for every player (the WC-066 gap). So every xG-driven view resolved to 0. Meanwhile **team-level xG IS available** (the API-Football overlay writes it to `m.teamStats[id].xG`) but was never used. Soundness bugs: (a) set-piece xG share divided **shot-level** set-piece xG by **player-level** total xG (mismatched sources/scales); (b) `conversion = goals/shots` counted penalties as shots. | Extracted the page's derivations into a testable `server/analytics.ts` and reworked it on the **no-fabrication** contract: all xG views now compute at the **team level, where the data is real** — Total xG, Avg xG/shot, a **team Finishing scatter** (team xG vs goals), and **team xG Leaders** — instead of empty player charts. Genuinely-absent panels (player xG-leaders, creativity when xA is 0, progressive/pressing when 0) are **hidden, not zeroed**, via availability flags. Fixed set-piece share to be **shot-level on both sides** of the ratio (scope-consistent, gated on `hasShotData`). Hermetic test asserts `stats.teamXG` equals the independent team-xG sum and each finishing point's goals equal the team's actual finished-match goals. 116 tests, tsc + build green. · analytics.ts · analytics/page.tsx |
| WC-078 | Clash of the Civilizations showed stale, group-stage-frozen data in the knockout round: team badges still said "Q" for sides already knocked out, cards read "N of Y through to R32" even for teams now in the quarters, and the region records + "Goals around the world" counted only group games while the goal-timing/H2H/clean-sheet panels counted every match (so goal totals mismatched and undercounted). | server/civilizations · civilizations page | Two causes. (1) Team badges used the frozen group-stage qualification `status` (`Q`/`E`), never updated for knockout results. (2) The region record (played/won/drawn/lost/GF/GA) was summed from `standingsByTeam`, which only covers the 3 group games — so knockout goals were invisible, inconsistent with the timing/H2H/clean-sheet panels that iterate all finished matches. | (1) Compute a **phase-aware per-team state** (`champion`/`alive`/`out`, penalty-shootout-aware) from finished knockout matches; the badge now shows 🏆 / IN / OUT correctly, and cards show "N still in · M reached the R32" instead of the frozen "through to R32". Added a per-region `stillAlive` count and made `eliminated` the current out-count (group + knockout). (2) Build the region record from **all finished matches** (group + knockout), so goals/W-D-L are full-tournament and consistent with every other panel; kept points/PPG as the group-stage concepts they are (own denominator), and fixed `finishing = goals − xG` scope mismatch (goals were group-only, xG all-tournament). Also `knockoutProb` now uses `reachR32` to match the "knockout reach" label. Hermetic test asserts region goals/played equal the all-finished-match totals. 112→113 tests, tsc clean. · civilizations.ts · civilizations/page.tsx |
| ENH-2 | Migrate live feed off SportMonks before the trial lapsed (2026-07-01) without losing data (enhancement) | data providers / load path | SportMonks trial ended; its paid tier was costly. API-Football (already paid) covers scores/squads/stats/events/xG but lacks a few SportMonks-only fields: player foot, deeper scouting metrics (big chances, progressive passes, ball recoveries, touches), coach careers, and played-match tactical stats (possession, PPDA, field tilt, formations, referee). | Froze those static/slow-changing fields ONCE from SportMonks into `cache/sportmonks-frozen.json` (48 teams, 1250 players, 76 matches), and overlay them onto the live API-Football snapshot by canonical name + shirt (`frozenOverlay.ts`). Verified 100% join: 349 feet, 1251 stat lines, 48 coach careers, 76 match stat sets. Future matches (past the freeze) get possession/shots/formations/referee live via `attachApiFootballMatchStats` (PPDA/field-tilt degrade — not in AF fixture stats). Flipped `live-2026` source `sportmonks`→`apifootball`. Permanent losses: weather + TV listings only. · frozenOverlay.ts · apiFootball.ts · loadTournament.ts · tournaments.ts |
| ENH-1 | Live scoreboard didn't auto-update in the browser (enhancement) | match/live pages (client) | Server data refreshed every 60s, but the browser only showed it after a manual reload — no client-side poll. | A global `<LiveRefresh>` (mounted in the Topbar) polls a no-store `/api/live-status` probe (cached snapshot only, no provider call) and `router.refresh()`es whenever the snapshot generation changes, so every page re-renders in place without a reload. Polls 15s while a match is live, 60s idle; pauses on a hidden tab; catches up on refocus. A freshness pill shows "N live · updated Ns ago" (live) / "Live" (idle). Server poll also made adaptive + env-tunable (`LIVE_REFRESH_MS` / `LIVE_REFRESH_LIVE_MS`, 30s while live). · LiveRefresh.tsx · api/live-status · instrumentation.ts |
| ENH-3 | "Ask the data" couldn't understand phrasings outside its fixed keyword vocabulary — idioms and nicknames ("who saw red for the Yanks", "top marksman", "best shot-stopper") fell to the generic "couldn't map that" reply (enhancement) | ai/llmParse · api/ask | The deterministic parser (nlq.ts) matches a fixed synonym set; anything outside it returns `unknown`, and you can't hand-enumerate every idiom. | Added an **LLM understanding layer** as a translator, never an answerer: when — and only when — the deterministic parser returns `unknown`, Claude (Haiku, `temperature:0`, structured output) classifies the question into a typed intent, which is rendered into a canonical query string the existing engine already understands and executed against real data. The model never emits a statistic, so it can't fabricate one; the engine still computes every number. Strict-improvement by design: key-gated (no `ANTHROPIC_API_KEY` → pure deterministic, verified by a hermetic test), 2.5s timeout with deterministic fallback on any error, and it never touches a query the parser already answers. Raw fetch (no SDK), mirroring narrate(). Verified live: "seen red the most" → red-card leaderboard, "deadliest finishers" → golden boot, "best shot-stopper" → saves leaderboard; comparisons/paths left untouched. · llmParse.ts · ask/route.ts |
| WC-076 | The Track Record page was hardwired to a 3-way home/draw/away framing — a "Draw" column, "coin-flip of 3" baseline, "a draw" labels — which is meaningless in the knockout round (nobody draws; a side advances). Also added data-science depth. (enhancement) | server/trackRecord · track-record page · server/lab | The grader used H/D/A for every match, so a knockout tie (e.g. Egypt 1-1 Australia, decided on pens) was scored as a "draw" against a 3-outcome baseline, and the page led with draw language. | Made the grader **phase-aware**: group games stay 3-way H/D/A; knockout ties are graded on the model's 2-way **advance** probability (`advanceProbabilities`, penalty-shootout-aware) against who actually went through, with a per-match baseline (0.5 two-way vs 0.667 three-way) so Brier skill still aggregates fairly. Split the graded table into "Knockout calls" (advance) and "Group-stage calls" (H/D/A), added a per-phase hit-rate split, and a **calibration/reliability** panel (predicted vs observed hit rate per confidence band — the key DS check that a 70% call happens ~70% of the time). Fixed the Model Lab's calibration consumer for the new row shape. Verified: a KO pens tie grades as a 2-way advance call to the shootout winner, no draw cell. · trackRecord.ts · track-record/page.tsx · lab.ts |
| WC-075 | On a live-feed outage (e.g. an API-Football quota reset mid-tournament), the app dropped all the way to the placeholder **simulation** — real data vanished for the length of the outage. (enhancement) | server/liveSnapshotCache · loadTournament · instrumentation · SiteBanner | The boot path had only two outcomes: healthy live, or the built-in simulation. There was no memory of the last good live snapshot — and Render wipes disk on every deploy, so the redeploy that triggers a failed boot also erases any on-disk cache. | Added an Upstash-backed **last-known-good cache** (Upstash survives redeploys; already used for the prediction log). Every successful full live load persists a gzip-compressed snapshot; on a failed boot the app **restores it instead of the simulation** — real, if stale, data labelled "Live · cached (updated HH:MM UTC)". The refresh loop rebuilds to fresh live once the feed heals (and the rate-limit back-off, WC-073, prevents hammering meanwhile). The banner (WC-072) shows a distinct "live updates paused, showing recent data" message for the cached state. Fully gated on the Upstash env (graceful no-op otherwise); zlib loaded via runtime require so instrumentation.ts still bundles for edge. Hermetic tests cover the gzip round-trip + unconfigured no-op. · liveSnapshotCache.ts · loadTournament.ts · instrumentation.ts · SiteBanner.tsx |
| WC-074 | A match review for a penalty-decided tie (Egypt 1-1 Australia, Egypt won on pens) said "the better side won" — the opposite of what happened (Australia had the xG edge and lost); a related bug called a 1-1 with no scorer data "a goalless affair". | ai/narratives | (1) The xG verdict compared `hStat.xG > aStat.xG` to `m.homeScore > m.awayScore`, but for a pens-decided 1-1 the score half is always **false** (a draw), so it never saw the shootout winner and the "deserved vs against the run" verdict came out backwards. (2) "A goalless affair" was the fallback whenever there were no scorer *names*, firing even for a 1-1 whose event timeline hadn't backfilled. | (1) Compute the verdict against the ACTUAL winner including penalties (`xgWinner = home/away by score, else penWinner`); it now reads "against the run of expected goals" when the loser out-xG'd the pens winner. (2) Only say "goalless" when the match truly finished 0-0; a scored match with no scorer names just omits the line. Hermetic test: a 1-1 where the home side out-xG'd the pens winner reads "against the run…", never "the better side won" or "goalless". · narratives.ts |
| WC-073 | API-Football quota got burned far faster than it should, and once exhausted the app kept hammering the spent budget (which pinned it on the simulation and delayed recovery). | data/providers/apiFootball · loadTournament · instrumentation | Two amplifiers. (1) On a daily-quota error (200 + `errors.requests`), `af<T>` **retried 5 more times** and then returned an empty array instead of failing — so every endpoint fired 6 requests, and a full snapshot (~60 endpoints) fired ~360. (2) The boot-recovery loop re-attempted a full snapshot **every 60s** while stuck on the simulation, so an outage meant ~360 requests/minute of pure waste. | (1) `af<T>` now detects a rate/quota error, throws a typed `ApiFootballRateLimitError` immediately (no retry), and sets a global 15-minute back-off. 429s get 2 quick retries then back off. (2) `refreshLiveScores`, `rebuildLiveSnapshot`, and the boot-recovery retry all honour the back-off window, so during an outage the app re-probes every ~15 min instead of every 60s — and picks up a daily reset within the window. Hermetic tests: a quota error now makes 1 request (was 6), and the back-off window opens/clears correctly. · apiFootball.ts · loadTournament.ts · instrumentation.ts |
| WC-072 | Visitors were shown the placeholder **simulation as if it were real** whenever the live feed fell back (e.g. a provider quota outage), with no indication, and there was no way to post a maintenance message during a build/bugfix. | app/layout · SiteBanner | The involuntary fallback to the simulation set the source label "Simulation (live feed unavailable)", but nothing surfaced it — `BootGate` only covers the initial boot window (`awaitingLive`), which goes false once live has booted once, so a *later* fallback showed the sim silently. | Added a `<SiteBanner>` (server component, top of the layout): (1) an env-driven **`SITE_NOTICE`** for a manual maintenance/announcement message we can set in the Render dashboard during a build or bugfix; (2) an **automatic** amber bar when the live edition is intended but the app is on the "live feed unavailable" fallback or still loading. Non-blocking (visitors can still browse history/lab/forecasts), and it auto-hides the moment live data returns because the existing LiveRefresh poll re-renders the server layout. · SiteBanner.tsx · layout.tsx |
| WC-071 | A knockout tie **still in a penalty shootout** showed as FINISHED, a goalless (0-0) full-time. | data/loadTournament · store · apiFootball adapter | Two issues. (1) The stale-live guard (WC-057) force-finished any match still flagged live past a **150-minute** window — but a tie that goes to extra time + a shootout runs ~2.5-3h from kickoff, so a genuinely-live shootout was coerced to a phantom 0-0 FT even though the feed still reported it live. (2) The API-Football adapter never mapped `livePhase`, so even a correctly-live shootout showed "LIVE 120′" rather than "Penalties". | (1) Widened the window to **3.5h** and, more importantly, added `shouldForceFinish()`: only coerce a stale-live match when the feed is **not** still reporting it live (a match in ET/pens has a fresh LIVE feed → trust it; only a dropped/stuck feed gets force-finished). Same 3.5h window applied to `getLiveMatches`. (2) Mapped `livePhase` from the AF status short code (`P`→PEN, `ET`→ET, `BT`→BREAK) — the MatchCard already renders "Penalties X-Y" and a shootout label from it. · loadTournament.ts · store.ts · apiFootball.ts |
| WC-070 | "Ask the data": "which African team will go the farthest" returned `unknown`; and the underlying `expectedFinish` forecast field was **inverted** (the strongest team got a 48th-place "expected finish"). | ai/nlq · analytics/simulate | (1) No "farthest / deepest run" query type existed, so a confederation-scoped depth question fell to `unknown`. (2) `simulate.ts` computed `expectedFinish = 49 - powerRank`, so the best side (powerRank 1) got 48 and the worst got 1 — backwards for a field documented as "expected finishing rank". | Added `farthestQuery`: ranks teams by projected **depth** (sum of stage-reach probabilities = expected knockout rounds survived — robust, straight from the Monte Carlo), optionally scoped to a confederation, and describes the modal round + reach ladder. Fixed `expectedFinish` to a real rank (1 = best); it was invisible in the UI but is the field I would have (wrongly) sorted on. Verified live: African → Morocco (ahead of Egypt, Cape Verde); global → Spain by depth; South American → Argentina. · nlq.ts · simulate.ts |
| WC-069 | The Daily Intelligence Briefing kept headlining Germany's 7-1 (a group-stage result) deep into the knockout rounds. | ai/narratives | The single-match highlights — "biggest rout" and "goal fest" — were computed over **all** finished matches with no recency filter, so the tournament's biggest scoreline (Germany 7-1, 18 days old) headlined forever. The briefing deck had the same ungated rout, and the ENH-5 momentum insight cards (comeback/possession-upset/clutch) were tournament-wide superlatives that also fed the briefing (a group-stage Paraguay upset surfaced as the headline). | Gated all match-highlight briefing content to a 3-day recent window (matching the existing upset-insight logic), while leaving **cumulative** team tallies (top scorers, meanest defence) over all matches. Fixed both `generateDailyBriefing` and `generateBriefingDeck`, and recency-gated the three momentum **insight cards** (the "Ask the data" momentum queries stay tournament-wide — only the daily cards are gated). Verified live: rout now "France 3-0 Sweden", headline forward-looking (Canada v Morocco R16); no 7-1 anywhere. · narratives.ts |
| ENH-5 | Extend the momentum feature: clutch/late-goal query + surface all three momentum stories as auto-detected insight cards (enhancement) | analytics/momentum · ai/nlq · ai/narratives · insights page | ENH-4 added comeback + possession-upset queries inline in nlq.ts, but there was no clutch-goal query and the stories weren't surfaced on the `/insights` page. | Extracted the detection logic into a shared `analytics/momentum.ts` (`comebackWins`, `possessionUpsets`, `clutchGoals`) as one source of truth. Added a **clutch-goals** query: late (85′+) decisive strikes — equalisers and go-ahead goals ("winner" if that side won — from the goal timeline, own goals credit the other side. Refactored the ENH-4 queries onto the shared detectors (comeback now event-based when events exist, so it catches deeper in-match deficits, not just the HT gap). Added three auto-detected **insight cards** (biggest comeback, starkest possession upset, standout clutch goal) with new `comeback`/`clutch` insight kinds + tones. Verified live: Gonçalo Ramos 94′ R32 winner vs Croatia; Belgium's comeback now correctly a 2-goal deficit. · momentum.ts · nlq.ts · narratives.ts · insights/page.tsx |
| ENH-4 | "Ask the data" had no way to surface momentum stories — comebacks and against-the-run-of-play wins — despite the data being present (enhancement) | ai/nlq | The app's only "momentum" was the ELO over/under-performance index; there was no comeback or possession-upset query, even though halftime scores + per-match possession are in the feed. | Added two intents. **Comebacks** (`comebackQuery`): finished matches where the winner came from behind — `trailedThenWon` checks the goal timeline (ever behind on the running score) when events are present, else the halftime score; own goals credit the other side, shootout kicks excluded. **Possession upsets** (`possessionUpsetQuery`): winners with <50% possession, sorted starkest-first. Scores shown from the winner's perspective; placed before the tactics rule so "possession-dominant" still routes to tactics. Verified live: 7 comebacks (Belgium 3-2 over Senegal from 0-1 down, R32), 14 possession upsets (Paraguay won 1-0 on 21% of the ball). · nlq.ts |
| WC-067 | "Ask the data": "who tops group H" said "Spain **top** Group H" (present tense) during the knockout stage — the same stale-phase issue as WC-062, but for the group-standings query. | ai/nlq | `groupStandingsQuery` hardcoded present tense ("X top Group Y") with no phase awareness. Once the knockouts start, the group table is final history. | Extracted the WC-062 "have the knockouts begun?" check into a shared `knockoutsBegun()` helper (any non-group match past SCHEDULED) and used it in both places: group standings now read "Spain **won** Group H with 7 pts from 3" once the KO rounds start, "Spain top Group H…" during the group phase. Verified live + 100 tests. · nlq.ts |
| WC-066 | "Ask the data": player xG showed as **0** everywhere (a 6-goal striker with "0 xG" in the Golden Boot table, "outscoring an xG of 0.0" in breakout blurbs, "0.0 xG" in player lookups). | ai/nlq · narratives | Player xG isn't in the API-Football feed **and** was 0 in the frozen SportMonks capture too (the xG field never captured) — so it's genuinely unavailable, not just missing live. xA, by contrast, is present (modeled, 562 players) though frozen at capture. Showing 0 as if it were a real xG is misleading; deriving/faking an xG value would violate the no-fabrication rule. | Graceful degradation (mirrors WC-021 / PlayersExplorer's `hasXg`): drop the xG column from the Golden Boot table when it's all zero (projection is pace-based, unaffected); omit the xG mention + row from a player lookup when 0; guard the breakout narratives so they don't claim "outscoring an xG of 0.0" or an empty shot-quality percentile. "most xG" already degrades to "No xG recorded yet" (WC-064) — fixed its acronym casing too. A **round-2 audit sweep** (comparisons with real names, per-90, demonym+position+metric combos, tactics, possession, extremes) otherwise came back clean. Verified live + 99 tests. · nlq.ts · narratives.ts |
| WC-065 | "Ask the data" audit, round 2: three follow-ups. (a) clean sheets were absent (WC-064d showed 0 for all) because API-Football has no clean-sheet field; (b) "who plays today" ignored "today" and showed the generic next 8; (c) the attack/defense ratings sat on mismatched scales ("strongest attack 34/100" next to "defense 100/100"). | ai/nlq · frozenOverlay · loadTournament | (a) No clean-sheet field in the AF player feed. (b) The fixture query had no date filter and "does X play today" didn't even route to it. (c) The raw offense/defense ratings (which feed the Poisson model) land on different effective scales when xG data is sparse — offense compresses low, defense inflates high. | (a) `deriveCleanSheets`: count each team's finished shutouts and credit them to its first-choice keeper; run in the live load path (26 keepers credited; "most clean sheets" → Unai Simón 4). (b) `fixtureQuery` filters to today's date on "today"/"tonight" ("Today's 1 match: Portugal v Croatia"; "does Brazil play today" → "Brazil do not play today"), and the dispatch now catches "play today". (c) Rescale offense/defense to a field-relative 0-100 for **display only** (raw values still feed the model), so both units share a scale and the leader reads 100. Verified live + 98 tests. · frozenOverlay.ts · nlq.ts · loadTournament.ts |
| WC-064 | "Ask the data" audit: four bugs found by sweeping query types. (a) "easiest path to the final" crowned early-exit teams (Algeria — 1.3% to reach the final); (b) "hardest path" returned the *easiest* answer; (c) "most likely to win the golden boot" returned the title favourite, not the scorer race; (d) "most clean sheets" said "leads all players with **0**". | ai/nlq | (a) Path difficulty averaged opponent ELO only over the rounds a team *appeared* in the projected bracket — a side projected to lose the R32 had a one-game "path" and looked easiest of all, backwards. (b) The generic `path`+`final` catch-all was checked before the hardest branch and stole it. (c) `most likely to win` matched the title query before the golden-boot check. (d) A descending leaderboard crowned a 0-value leader when the metric had no data (clean sheets aren't in the API-Football player feed). | (a) Rewrote as `routeToFinal`: walk the bracket from a still-alive team's current slot to the final, taking each round's opponent from the sibling sub-bracket's projected winner — a full, equal-length route for every team; eliminated sides return null (no path). Now Argentina (32% to reach the final) tops it, Mexico is toughest. (b) Check hardest before the catch-all. (c) Guard the title query against `golden boot`/`scorer`. (d) A descending leaderboard whose leader is 0 says "No X recorded yet" with empty rows. Verified live + 96 tests. · nlq.ts |
| WC-063 | "Ask the data": "tell me about Congo's goalkeeper" returned nothing | ai/nlq · resolver | Two gaps. (1) The team is named **"Congo DR"** in the feed; the resolver matched only full name, 3-letter code, or long aliases, so the bare word "congo" resolved to no team. (2) There was **no "team's <position>" query type** at all — even for a valid team, "who is Belgium's goalkeeper" returned `unknown` and "Belgium goalkeeper" degraded to a generic team summary that ignored the position. | (1) Added a distinctive-token fallback to `extractTeams`: when nothing else matches, a name token ≥4 chars that belongs to exactly one team resolves it ("Congo"→Congo DR, "Korea"→South Korea, "Verde"→Cape Verde Islands) — uniqueness-gated so it never guesses between ambiguous names. (2) Added `teamPositionQuery`: a team + a position (and no metric) lists that team's players at the position ordered by minutes, leading with the first-choice name (keeper → saves/clean sheets; outfield → goals/assists). Verified live: "tell me about congo's goalkeeper" → "Lionel Mpasi is Congo DR's first-choice goalkeeper (360 min)…"; "Mexico's captain" correctly stays unknown (captain isn't a position). · resolver.ts · nlq.ts |
| WC-062 | "Ask the data": a team lookup ("how is Spain playing") said "**Currently** 1st in Group H with 7 pts" — but the tournament is in the knockout round, so the group table is history, not current status | ai/nlq | `teamLookup` hardcoded a present-tense group-standings line from `engine.standingsByTeam` with no awareness of tournament phase. That standing is the *final* group table; once the knockouts start, "Currently Nth in Group X" is stale and wrong. | Made the status line **phase-aware** (`teamStatusLine`): while the group phase is live it still reads "Currently Nth in Group X"; once any knockout match has kicked off it reframes the group result as history ("Finished 1st in Group H") and appends the team's actual knockout run, derived from their matches — into/through to the next round, playing now, eliminated in the R16, or World Cup champions (goals then penalties decide each KO tie). Added a "Status" row to the table. Verified live: Spain → "Finished 1st in Group H; through to the round of 16"; Argentina → "into the round of 32". · nlq.ts |
| WC-060 | "Ask the data": "which players got red cards in the round of 32" returned a generic global card list, missing Balogun's R32 red | ai/nlq | Two gaps: (1) the leaderboard had no **match-stage** filter, so "in the round of 32" was ignored; (2) it ranked by the **season aggregate** (`redCards`), which lags/omits — Balogun's 64' R32 red card was in the event timeline but not yet his card tally. | Added stage detection (`detectStage`) + an **event-derived** tally (`stageEventLeaderboard`): for countable events (goals, red/yellow cards, assists) with a stage in the query, count straight from the match events of that stage, respecting scope + position. This applies the stage filter AND catches what the aggregate misses. Also added scorer synonyms ("scored", "scorers"). Verified live: "red cards in the round of 32" → Hincapié 1, Balogun 1 (from 10 R32 matches' events). · nlq.ts |
| WC-059 | "Ask the data": "which spanish player has the most goals" returned the generic global top scorers | ai/nlq · resolver | "most goals" routed to `goldenBootQuery`, which applies no filter; and the team detector only knew team names, codes, and a few noun aliases, so nationality **adjectives** ("spanish", "brazilian") never resolved to a team. Confederations and clubs weren't recognized at all, and leaderboards only sorted descending. | Added a `detectScope()` semantic layer: **demonyms** → nation (48-team map), **confederation/region** words → team set, and **club/league** names → player filter (via the surname+dob crosswalk, warmed at boot). Applied it to the goals/leaderboard and youngest/oldest queries; a scoped or positional "most goals" now reroutes from the golden-boot race to a filtered leaderboard. Also added ascending sort (`fewest/least/lowest`) and a few metric/position synonyms (playmaker, shutouts, dirtiest, mids, fullbacks). Verified: "which belgian player has the most goals" → Belgium-scoped; "european … assists" → UEFA-scoped; "fewest goals" → ascending. · nlq.ts · instrumentation.ts |
| WC-058 | Knockout (playoff) matches showed a **draw %** prediction — but a knockout can't end level | MatchCard · match page · format | `predictMatch` (bivariate-Poisson) buckets every match into home/draw/away for the 90-minute result, with no awareness of stage. Correct for the group phase; misleading for knockouts, where a level score after 90 goes to ET + penalties and someone still advances. | Added `advanceProbabilities()`: folds the draw into each side's chance to **advance**, split by their normal-time win ratio (stronger side likelier over ET+pens; 50/50 on a dead heat; sums to 1). MatchCard + match page branch on `stage !== 'GROUP'` to show a two-way "to advance" bar instead of W/D/L; group games unchanged. Core engine untouched (display-only). · format.ts · MatchCard.tsx · matches/[id]/page.tsx |
| WC-057 | A match that ended last night still showed as **LIVE at 68′** (Mexico 2-0 Ecuador) | loadTournament refresh · store | The match booted into the snapshot while genuinely live at 68′ (kickoff 01:00, boot ~02:08 = 68 min in). API-Football later marked it FT, but the app never corrected it: once a match falls past its refresh window (`kickoff + 150min`), `needsRefresh` stops firing for it, so a stuck live status is frozen forever — and `getLiveMatches` had no kickoff guard, so it still counted as live. | Two-part self-heal: (1) `refreshLiveScores` now triggers on and **coerces a stale-live match to FINISHED** (minute 90) once it's past its window, overriding a lagging feed status; (2) `getLiveMatches` requires kickoff within ~2.5h, so a stale live status never counts toward the live badge/ticker. Verified: Mexico-Ecuador was FT/90 in API-Football; app had it LIVE/68. · loadTournament.ts · store.ts |
| WC-056 | Every live player's six attribute bars showed a flat **78** (regressed WC-023) | apiFootball adapter · player page | API-Football carries no FIFA-style per-attribute ratings, but the adapter's `newPlayer` fabricated all six (pace/shooting/passing/dribbling/defending/physical) at 78 plus a flat overall 78. The SportMonks path (WC-023) had fixed this by omitting the six so the player page shows a real Match Rating card (`hasAttrs = typeof r.pace === 'number'`); the API-Football adapter predated that fix, so the migration reintroduced it. | Mirror WC-023 in `newPlayer`: omit the six per-attribute ratings and seed `overall` from a team-strength prior (`(attack+defense)/2`) instead of 78 — the frozen overlay upgrades it to the real SportMonks match rating. Verified live: players-with-pace 0 (was all), 21 distinct overalls (Messi 88, Mbappé 89, Ochoa 77), page shows Match Rating not flat bars. · apiFootball.ts |
| WC-055 | Scorer tallies wrong on live data (Mbappé showed 4 goals, actually 6) | loadTournament · frozenOverlay | API-Football's WC feed is unreliable two ways at once: its per-player **season aggregate lags** a just-finished match (still 4/3apps after France's 4th game), AND its **event feed drops** some earlier goals (events totalled only 4 across all 4 France games). So neither AF stream alone reaches 6 — and a naive max(aggregate, events) still gives 4. SportMonks had it right (4 through the first 3 games). | `reconcileScorers` (frozenOverlay): `goals = max(AF aggregate, frozenSportMonksBaseline + goals-from-events-in-matches-newer-than-the-capture)`. The frozen baseline is accurate through capture time; live events cover matches since; disjoint windows + an outer max ⇒ no double-count, only ever raises a tally. Shootout kicks excluded by minute>120; missed pens already PENALTY_MISS. Wired into refreshLiveScores + rebuildLiveSnapshot (which now also carries event timelines across the rebuild). Verified live: Mbappé 4→6, injected 125' shootout goal ignored. · frozenOverlay.ts · loadTournament.ts |
| WC-054 | Player names showed the full legal name ("Francisco Guillermo Ochoa Magaña") — 62% were 3+ words | apiFootball adapter · types · resolver | The adapter built `name` from API-Football's `firstname`+`lastname`, which hold the full registered name (every given name + both surnames). | Derive a **display name** from `player.name` (which encodes the known name as "Initial. Surname", e.g. "G. Ochoa") by expanding the initial against the firstname tokens → "Guillermo Ochoa". Mononyms/clean commons used as-is. Store the full name in `fullName` (new field) for search + the club crosswalk; resolver now scores both. Verified live: 3+-word names 62%→7%; samples correct (Lionel Messi, Weston McKennie, Thibaut Courtois). · apiFootball.ts · types.ts · resolver.ts |
| WC-053 | Club Connections / Discoveries silently matched ~nobody after the API-Football migration (regressed WC-024's 703 links) | apiFootball adapter · clubs · discoveries · frozenOverlay | The club crosswalk keys on **surname+birthdate** (`clubMatchKeys` returns `[]` without a dob), but the API-Football adapter never set `birthDate` — so every live player had no dob → no club match. | Capture `player.birth.date` from API-Football's `/players` payload; backfill the ~1% stragglers from the frozen overlay; clubs/discoveries now key on `fullName ?? name` (all surname tokens). Verified live: players-with-dob 100%. · apiFootball.ts · frozenOverlay.ts · clubs.ts · discoveries.ts |
| WC-052 | Team page: clicking a player in the "Last XI" (incl. subbed-in names) hit a broken 404 | server/lineups · teams/[id] page | The XI chips link to `/players/[id]`, but after the API-Football migration `m.lineups` is supplied by the frozen overlay — which re-keyed the team but left the inner player ids as **SportMonks** ids (`code-smId`), while the live store uses API-Football ids (`code-afId`). Every lineup id dangled → `getPlayerView` undefined → `notFound()`. | `lineupView` now repairs each lineup id against the live store: keep it if valid, else re-resolve by **surname + first initial** within the team, accepting only a UNIQUE candidate (a squad often shares a surname — Ivory Coast: Seko vs Yahia Fofana, Yan vs Ousmane Diomande — and a mis-link is worse than no link). No match → keep original id for stable keys but mark `linkable:false`; the page renders those as plain text, never a 404, never a wrong player. Verified live: 69% link correctly, 31% safe plain-text, 0 mis-links. **Follow-up:** re-captured the frozen overlay WITH jersey numbers in the lineups, so `frozenOverlay` re-keys lineup ids to live ids by team+shirt (exact) → 100% resolve; `lineups.ts` name-repair stays as the fallback. · lineups.ts · teams/[id]/page.tsx · frozenOverlay.ts |
| WC-051 | Most players (incl. Messi) showed **0 goals / no stats** after the API-Football migration | apiFootball adapter | The league players-stats endpoint (`/players?league=1&season=2026`) paginates ~63 pages (20/page), but the overlay loop was capped at `page <= 15` — so only the first ~300 of 1266 players got real stats; everyone on a later page kept `emptyStats` (0 goals/apps). Latent under SportMonks (which aggregated stats from lineups), exposed once API-Football became the live feed (ENH-2). | Drive the loop off the reported `paging.total` (safety ceiling 80), retry 429s per page, and pace ~120ms between pages to stay inside the rate limit. Verified live: Messi 6 goals / 3 apps; players-with-apps 997 (was ~300), scorers 149. · apiFootball.ts |
| WC-001 | Every player shown as "CM" | data adapter | `detailedPosition` hardcoded to `'CM'` | Map role→detailed position (GK/CB/CM/ST) · pre-launch |
| WC-002 | `/live` crash — `reading 'flag'` | live/matches render | TBD knockout fixtures → `getTeam(id)!` undefined → `.flag` | Guard MatchCard + `liveMatches`/`matchDetail` filters · pre-launch |
| WC-003 | Home page "client-side exception" | home / narratives | `getPlayerView` used `getTeam()!` → undefined during live load | `getPlayerView` returns `undefined` when team unresolved; cascaded guards · pre-launch |
| WC-004 | Transient bracket crash — `reading 'teamId'` | analytics/bracket | `buildBracket` asserted seed slots (`!`) → undefined mid-load crashed the whole engine | TBD defaults for unresolved seeds · `dc47ea6` |
| WC-005 | Hollow live feed → blank app | data load / degradation | Quota/key returns team skeleton with no squads; no fallback | `isHealthyLive` check → serve full simulation if hollow · `434daf9` |
| WC-006 | "lionel messi" found nothing (matching) | search | Matched only the surname (last token) | Multi-token match, later full resolver · `434daf9` → `9287203` |
| WC-007 | `/api/search` 500 | search endpoint | `getTeam(m.homeTeamId)!.name` on a TBD fixture in the match filter | Resolve both sides up front, drop unresolved fixtures · `2c76f4a` |
| WC-008 | Live data loaded but rendered empty | store / caching | Module-scoped lookup indexes weren't invalidated across module instances when the `globalThis` snapshot swapped → stale sim index orphaned every live player | Key indexes to the snapshot identity; rebuild on swap · `f6d9f31` |
| WC-009 | Search false-positives / typo misses (live) | resolver | Long query matched a 2-char name fragment (`holland`→`Jun-Ho`); no transposition handling (`halaand`); fuzzy on short tokens (`kane`→`Sané`) | Require name token ≥4 for the extend rule; Damerau/OSA distance; gate fuzzy to ≥5-char tokens · `09682d9` |
| WC-010 | Search & NLQ used two different weak matchers | search + AI | Duplicated, inconsistent entity matching | Unified both behind one shared resolver + generative coverage corpus · `9287203` |
| WC-011 | Kickoff shown as "tomorrow 1pm" | formatting | `timeZone: 'UTC'` hardcoded; WC-2026 is in North America, so evening games roll onto the next UTC day | `<LocalTime>` renders each fixture in the viewer's own zone · `f05b844` |
| WC-012 | Live games never flipped to LIVE; scores frozen | data refresh | Feed fetched once at boot, never refreshed | Poll fixtures every 60s during play windows; merge status/score/minute · `f05b844` |
| WC-013 | Match report says a team "won" during a live game | narratives | `generateMatchSummary` only special-cased `SCHEDULED`; LIVE fell through to past-tense result | Add a LIVE/HALFTIME present-tense branch; reserve "won" for FINISHED · `c1603f0` |
| WC-015 | Match events missing (goals, disallowed/offside, cards) | data adapter / live refresh | Adapter hardcoded `events: []`; the per-fixture timeline was never fetched | Fetch `/fixtures/events`, map goals/cards/subs/VAR, resolve scorer+team from squads, surface VAR/own-goal in the UI; backfill finished matches once (capped) · `a445a67` · `968c8c2` · `3521d7c` |
| WC-017 | SportMonks: Australia/Austria & Iran/Iraq merged into one team | sportmonks adapter | Team code derived by slicing the first 3 letters of the name → `AUS`/`IRA` collisions merged distinct nations, corrupting groups | Use SportMonks' real `short_code` (AUS/AUT, IRN/IRQ) — also matches the enrichment table so flags/ELO populate · sportmonks adapter |
| WC-018 | `/predictions` 500 — `reading 'gf'` on SportMonks data | analytics/simulate | A finished match referenced a team not in its group's base map (group-assignment gap) → `base.get(id)!` undefined | Guard the group base lookup and skip rather than crash (same rule as WC-002) · simulate.ts |
| WC-019 | "Breakout" flagged 25-year-olds (Haaland) on SportMonks | narratives / nlq | SportMonks gives no player age (`age=0`), so the `age ≤ 23` filter passed everyone | Require `age >= 17 && age <= 23` — breakouts only surface when ages are actually known · narratives.ts · nlq.ts |
| WC-020 | Historical `/teams` page empty (no teams) on every datahub edition | datahub importer | Teams were built with `confederation: ''`, but the `/teams` page groups by confederation → every team filtered out | Populate `confederation` from teams.csv `confederation_code` (UEFA/CONMEBOL/CAF/…) · fetch-datahub.mjs |
| WC-021 | Betting Edge stuck on "odds loading…" forever (live) | betting / odds | Odds were keyed by API-Football fixture ids, but post-migration match ids are `m-<SportMonks id>` — two id namespaces that never join. SportMonks plan isn't entitled for odds (403). | Decoupled odds into a source-selectable market layer joined to fixtures by **team + kickoff date** (with home/away orientation + alias map for divergent nation spellings). Primary source = **API-Football** `/odds` (Pro plan, 14 books, scoped to betting only — does NOT change DATA_SOURCE); **The Odds API** kept as automatic fallback. Honest empty-state copy + unmatched-event diagnostic. · betting.ts · oddsApiFootball.ts · oddsApi.ts |
| WC-016 | Advanced metrics show 0 / "0th percentile" on live | many UI surfaces | The live feed doesn't carry every advanced metric, but surfaces rendered the empty values as real (`0` / `0th percentile`). Largely resolved by the SportMonks migration (real xG/shots/progressive/tackles) + the player-percentile omit (store.ts skips uniformly-0 metrics). Remaining team/shot-level zeros fixed: **Home "Total xG"** now sums real player xG (not empty per-match team stats); **standings** hide the xG line when team xG is absent; **analytics** recompute xG/shot, conversion, big chances from player aggregates, swap "Set-piece xG" → "Total xG" and hide "Pressing Volume" when shot/pressure data is missing. Verified live (real values) + seeded (full detail). · page.tsx · standings · analytics · store.ts |
| WC-024 | Club Connections (and Discoveries) showed only ~11 players on live data | clubs · discoveries · club-affiliation precompute | The `clubs.json` map was keyed by **API-Football** player ids, but live-2026 migrated to **SportMonks** ids — two namespaces that never join, so only ~11 coincidental numeric collisions matched (and could point at the wrong club). | Rebuilt the join as a cross-provider crosswalk on **surname + birthdate**: the precompute now pulls full bios (name/DOB/nationality) via API-Football `/players?team=&season=` and emits a `byKey` (`"surname\|dob"`) map; SportMonks players now carry `birthDate`; a shared key builder (script + app) generates the keys and `clubs.ts` + `discoveries.ts` join on them. Each player is indexed under **every surname token** (DOB-gated) so compound names ("García López", "Rúben Santos … Dias") match whichever token a provider treats as the last name. Verified live: **703 players linked (was 11)**, high precision (Bayern 14/14, Liverpool 9/9 incl. Salah; the lone miss is an upstream API-Football affiliation quirk). · fetch-clubs.mjs · clubAffiliations.ts · clubs.ts · discoveries.ts · sportmonks.ts · types.ts |
| WC-023 | Live data fabricated flat stats: every player's attributes all **76**, and every match narrative claimed **50% possession / 50% field tilt** | sportmonks adapter · narratives · player page | SportMonks carries no FIFA-style player attributes and no possession/field-tilt; the adapter stamped a flat `76` on every player's `rating` and `generateMatchSummary` used `?? 50` fallbacks — so placeholders rendered as real data on every live player and match. Same class as WC-016 (fabricate instead of degrade). | Stop fabricating, degrade honestly: made the six per-attribute ratings **optional** and omitted them for live; player `overall` now comes from SportMonks' **real average match rating** (team-strength prior only until a player features); the player page swaps the Attributes bars for a real **"Match Rating /10" card** with an explanatory note when attributes are absent; the match narrative omits the possession/field-tilt line unless `teamStats` actually carries it. Verified live (Messi 9.7/10, no 76 wall; "Mexico won 2-0" narrative has no possession line) and seeded bars intact. · narratives.ts · sportmonks.ts · players/[id]/page.tsx · types.ts · generate.ts |
| WC-022 | Value bets surfaced longshot trolls (e.g. "Iraq beat France" as top pick) | betting / model-vs-market | `valueBets` ranked by raw `ev = model*odds - 1`. The seeded Poisson overstates underdog tails (compressed, coarse hand-seeded priors), and at long odds a tiny prob gap explodes the EV (model 20% vs market 8% at 12.0 → +140% EV), so longshots dominated the list while the market (far sharper) was right. Not a data bug — the model was simply wrong and the EV sort rewarded it. | Two guardrails in `betting.ts`: (1) `shrunkEv()` shrinks the model toward the de-vigged market as the price lengthens (full weight at evens → zero by odds 12), taming the displayed/ranked EV while leaving the raw `edge` honest; (2) `MIN_MODEL_P` floor (0.12) — never flag value on an outcome the model itself rates a longshot. `valueBets` now gated by both. · betting.ts |
| WC-028 | Player stats went **stale mid-tournament** (e.g. Mbappé stuck on 2 goals / 1 app while France had played 2) | data refresh · loadTournament | Per-player aggregates (goals, apps, xG…) are built only by the full `fetchSportMonksSnapshot` at boot. The 60s `refreshLiveScores` patches match **scores + event timelines** in place but never re-aggregates player stats — so once a match finished *after* boot, its scorers' tallies (and the golden boot, top scorers, scoring-streak insight) froze until a redeploy. Proof: match list showed 2 France games finished, but Mbappé had `appearances: 1`. A fresh fetch correctly gives 4 goals / 2 apps. | When `refreshLiveScores` sees a match newly flip to FINISHED, trigger `rebuildLiveSnapshot()` — a guarded, best-effort background full re-fetch that recomputes every player's stats from all finished matches (idempotent; runs a few times a day at most; keeps the current snapshot on failure). Deploying the fix also boots a fresh snapshot, so the stale figures correct immediately. · loadTournament.ts |
| WC-027 | Live data was missing all **team match stats** (possession, shots, passes…) and every player was shown **right-footed** | sportmonks adapter · narratives | We only fetched `lineups.details;events.type` per fixture and `players.player` per squad, so `teamStats` stayed `{}` (WC-023 then *omitted* possession/field-tilt assuming the feed had none) and `foot` was hard-coded `'right'`. But SportMonks **does** provide both: a `statistics` include returns 40+ team stats per fixture (Ball Possession %, Shots Total/On Target, Passes, Successful Passes %, Big Chances, Corners, Dangerous Attacks, Tackles, Interceptions, cards…), and player `metadata` (type_id 229) carries real preferred foot. | Pull them: added `statistics.type` to the per-fixture detail call and mapped it into `MatchTeamStats` by home/away location (possession/shots/passes/cards direct; **xG** summed from that fixture's lineup player xG; **field tilt** ≈ share of dangerous attacks; **PPDA** ≈ opp passes / our tackles+interceptions); added `players.player.metadata` to the squad call and mapped foot via `footOf()`. The recap "underlying numbers" + possession lines (WC-023) now light up on live, gated on real presence. Also fixed the same under-fetch for **venue** (every live match showed `venue: 'TBD'` — added the `venue` include → real stadium + city) and added **starting formations** (`formations` include → `match.formations`, shown on the match page, e.g. 4-1-4-1 vs 5-3-2), **referee** (`referees.referee` include, main official type_id 6 → `match.referee`) and **weather** (`weatherreport` include → `match.weather` {tempC, description}), both surfaced under the match-page scoreboard. Verified live: all 40 finished matches carry real stats (MEX 61% / RSA 39% possession, xG 1.3/0.1), venue (Mexico City Stadium), formations, referee (Wilton Pereira Sampaio) + weather (25°C, moderate rain); foot 140R/53L/7both (Messi/Oyarzabal left). · sportmonks.ts · narratives.ts · matches/[id]/page.tsx · types.ts |
| WC-026 | "Emerging player" news showed **€0m valuations** all over live/historical data | narratives · nlq | Only the seeded edition computes `marketValueEur`; every external source (SportMonks, datahub, StatsBomb, API-Football, football-data) sets it to `0`. The breakout insight body and the NLQ breakout answer/table interpolated it unconditionally, so live/historical rendered "€0m valuation" — and the one breakout insight propagates to `/insights`, the home AI Insights panel, and the rotating briefing deck, so it read as "littered". Same class as WC-016/WC-023 (placeholder shown as real). | Degrade gracefully: omit the valuation clause when `marketValueEur === 0` (breakout insight body, scouting report already guarded); the NLQ drops both the "€Nm valuation" phrase and the **€m** column when no candidate has a value (`hasVal`). Seeded edition still shows real values (€14m etc.); live/historical simply omit. Verified live (no €0m, no valuation clause) + seeded (values intact). · narratives.ts · nlq.ts |
| WC-025 | Clicking a player (or team) on `/defense` could land on a broken/empty page on live data | store · player/team detail pages | `getPlayerView(id)` returned `undefined` when a player's team didn't resolve (snapshot mid-swap / cross-index staleness). The defense **list** (memoized `getPlayerViews`) still showed the player, but the **detail** page turned that `undefined` into `notFound()` → an empty page. The detail page also kept a crash-prone `getTeam(p.teamId)!`. Intermittent + live-only (the 60s refresh window), so it never reproduced in a steady snapshot. Same class as WC-003/WC-008. | `getPlayerView` now builds the view with a **minimal fallback team** (id/name/code/flag from the player's own `teamId`) instead of returning `undefined`, so a real player is never dropped from the list and never 404s on its page (the real team fills back in next render). Player page derives `team` safely (no `!`, falls back to the view's lightweight team + neutral accent); team page skips fixture rows whose opponent is momentarily unresolved instead of `getTeam(...)!`. Verified: real team chrome still renders, bogus ids still 404. · store.ts · players/[id]/page.tsx · teams/[id]/page.tsx |
| WC-014 | Search runner-up noise on multi-word names — `lionel messi` surfaced weak extras (`L. Bessi`, even `Lionel Scaloni`) under the correct top hit | search · resolver | Two faults compounded. (1) `tokenScore`'s fuzzy branch misread the bounded edit-distance: `editDistance` returns `max+1` as its over-budget sentinel, but for a 5-char token (budget 1) that sentinel is exactly **2**, which the `d === 2 → 0.45` branch read as "two typos" — so any 5-char query token leaked a spurious 0.45 against almost any word (`messi`↔`scaloni`), matching everyone named Lionel. (2) An initial-only first-name match (full "lionel" hitting the stored "L" = 0.5) plus a one-edit surname fuzz (`messi`↔`bessi` = 0.62) cleared the 0.55 bar on weak evidence alone. | (1) Trust the fuzzy distance only when it's genuinely within budget (`d <= max`), so the sentinel can't pose as a match. (2) Require at least one **strongly** matched word (exact or prefix, score ≥ 0.7) once a query carries two+ significant tokens — an initial + fuzzy combo no longer resolves. The real player still wins; runners-up fall below threshold. Verified live (every star multi-word name returns exactly one clean hit; single-token + typo queries still resolve) + 2 regression tests over the generative roster corpus. · resolver.ts |
| WC-050 | **No extra-time / penalty support** — shootout scores never appeared and a pen-decided tie had no bracket winner | sportmonks · loadTournament · types · UI | `penalties` was hardcoded `null` in both the snapshot build and the live-update fetch, so a real `FT_PEN` tie (Germany 1-1 Paraguay, won 4-3 on pens) stored no shootout score — the bracket's `winnerOf` then returned null (level score + null pens) so the tie had no winner, and results/cards showed nothing. ET and the shootout also collapsed into `LIVE` (`MatchStatus` has no phase for them), so a live knockout couldn't show "Extra Time" or "Penalties". | Extract penalties from the SportMonks `scores` array (`description === 'PENALTY_SHOOTOUT'`) in both the snapshot and live-update paths; add a `livePhase` ('ET' \| 'PEN' \| 'BREAK') to `Match` + `FixtureUpdate`, mapped from the in-play state (guarded so finished AET/FT_PEN don't read as live ET) and merged on refresh (which now also reacts to phase/penalty changes). UI: LiveMinute shows "ET 105′" / "Pens" / "Break"; the ticker appends the live pen score; match cards show the phase live and "FT (pens)" when decided. Verified live: the Germany-Paraguay tie now carries pens 3-4, the bracket advances Paraguay with a "p" marker, and results show "3–4 pens". · sportmonks.ts · apiFootball.ts · loadTournament.ts · types.ts · LiveMinute.tsx · LiveTicker.tsx · MatchCard.tsx |
| WC-049 | **xG was 0 everywhere** on live (Messi 0.00 xG on 6 goals) | sportmonks · apiFootball · loadTournament | SportMonks (our primary live feed) gates expected goals behind the `xgfixture` include — our key returns *"You do not have access"* — so the adapter's `type_id 5304` read was always empty and every xG defaulted to 0. The xA shown is a separate, modeled estimate, which masked it. (Player-level xG has no source at all for the WC: API-Football only exposes xG per team, not per player; SportMonks gates it.) | Real **team** xG exists in API-Football (`/fixtures/statistics → expected_goals`, e.g. MEX 1.46 / RSA 0.07) on the plan we already have. Added `attachApiFootballXg` + `enrichLiveXg()` — a best-effort background enrichment (like the TV/H2H/coach ones) that pulls team xG and overlays it onto the SportMonks matches, joined by team name (+kickoff date), with a verified 5-entry alias map for divergent spellings (South Korea→Korea Republic, Czechia, Bosnia &→and, USA→United States, Ivory Coast→Côte d'Ivoire). Skips already-enriched matches; swaps the snapshot to rebuild the engine so standings xGFor/xGAgainst update. Home "Total xG" now sums real team xG (was summing player xG = 0, reversing the WC-016 workaround). Verified live: all 74 finished matches carry real team xG; standings + Total xG real. Player-level xG stays absent (no source). · apiFootball.ts · loadTournament.ts · page.tsx |
| WC-048 | App could **show the placeholder simulation on load** despite the BootGate | layout · liveStatus · BootGate | The gate only engaged while `loading` was true. If the boot-time live fetch *failed*, the app sat on the simulation with `loading:false` until the next self-heal retry (up to ~60s later) — and a page load in that gap rendered the sim with no gate. (Every deploy/cold-start also opens a normal boot window, which the gate already covers.) | `liveStatus()` now also reports `awaitingLive` — true when the deployment intends live (`DATA_SOURCE → live-2026`) but isn't on it yet and has never booted (the involuntary sim fallback, distinct from a user-chosen sandbox via the `__wcLiveBooted` flag). The layout holds the gate on `loading \|\| awaitingLive`, so the placeholder is masked through the whole pre-live window, including between retries. Never gates a genuine sim deployment, a booted-then-switched sandbox, or historical browsing. · queries.ts · layout.tsx |
| WC-047 | Knockout stage **wasn't clearly stated**, and the bracket showed *projected* ties during the real knockout | bracket · home · analytics | Two faults once the R32 draw landed (29 Jun): (1) `buildBracket` always re-seeded the tree from projected qualifiers under standard seeding — so during the actual Round of 32 it showed **fictional matchups** (e.g. France–Senegal) instead of the real draw, even though 16 real R32 fixtures (with results) existed in `matches`. (2) The **home page had no phase indicator** at all, and the "Live & Upcoming" panel still read the hard-coded subtitle *"Final round of group fixtures."* A user couldn't tell the tournament had reached the knockouts. | `buildBracket` now seeds its **first column from the real drawn fixtures** when they're complete — actual matchups, real winners + scorelines for played ties (penalties break a level tie), `matchId` linked — and projects the deeper rounds forward by ELO from there; it falls back to the projected tree before the draw lands. The bracket page shows the real scoreline on decided ties and a phase-aware header ("Round of 32 underway · N of 16 played · later rounds projected"). The home page gained a **Knockout Stage status banner** (current round + ties played + link to the bracket) and the panel subtitle is now phase-aware. 3 new bracket unit tests. · bracket.ts · analytics/index.ts · bracket/page.tsx · page.tsx · types.ts |
| WC-046 | Browsing a **past World Cup reverted to live-2026** after ~60s (players/teams/bracket vanished) | instrumentation · self-heal loop | The WC-041 boot self-heal re-activated live-2026 whenever the active tournament wasn't live — but its condition (`!== 'live-2026'`) couldn't distinguish "failed boot, stuck on simulation" from "user deliberately opened a past edition." So switching to men-2022/2018/Women's etc. got force-reverted on the next 15–60s refresh cycle, mid-browse — the past-WC data appeared not to load. | Gate the self-heal on a new `__wcLiveBooted` flag: only force-recover live until it has loaded **once**; after that, a non-live active tournament means the user chose it, so leave their selection alone (the refresh loop just no-ops that cycle). Verified: men-2022 now persists past the refresh cycle and its players (80 cards), teams (32), and bracket all render. · instrumentation.ts |
| WC-045 | Boot window **still fell back to the simulation** despite the WC-042 gate | BootGate | Two faults in the gate's reveal logic. (1) The hold was capped at **30s**, but the real live load (fixtures + 48 squads + events + enrichments) routinely runs ~25–40s on a cold Render instance — so the cap fired *before* live landed and revealed the sim. (2) The reveal condition was `loading === false`, which a **failed** load also satisfies (the flag clears but the simulation stays active), so a transient SportMonks hiccup revealed sim too. Net effect for the user: "it went back to simulated on boot." | Reveal **only** once the live tournament is genuinely active (`tournamentId === 'live-2026' && !loading`), so the gate holds through the WC-041 self-heal retries instead of unveiling the placeholder. Bumped the last-resort cap 30s → **90s** (only a prolonged, genuine failure now falls through), and added a "pulling latest fixtures…" reassurance after 14s. Verified: boot HTML keeps the overlay over the sim content, the gate holds until live-2026 lands, then swaps clean (no stranded overlay, no sim mention, console clean). · BootGate.tsx |
| WC-044 | **Pages crashed: `Cannot read properties of null (reading 'get')`** (the root cause behind the WC-040 recurrences) | data/store | `setDataset()` nulled the lazy lookup indexes (`_teamIndex` …) but left `_indexedSnap` pointing at the old snapshot, and `syncIndexes()` early-returned on snapshot identity alone. In a concurrent-refresh / cross-instance race you could land with `_indexedSnap === current snap` **and** a null index, so `teamIndex()` returned `null!` and every `getTeam()`/`getPlayer()` threw `null.get`. Pages reading the `getTeams()` array (/predictions) survived; everything calling `getTeam` (home, /matches, /results, every match card) crashed; narratives hit it too but caught it (the only thing that surfaced in the logs). | `syncIndexes()` now rebuilds when the snapshot swaps **or any index is null**, and `setDataset()` resets `_indexedSnap` so it can't leave a stale pointer. Reproduced the exact production error in a unit test (re-setting the same snapshot) and verified the fix clears it. · store.ts |
| WC-042 | Boot window **showed the simulation by default** during a live tournament | layout · BootGate | The app boots on the deterministic simulation and loads live data in the background (~25s); for that window, fresh page loads rendered the placeholder sim data (with only a small "Loading live data…" pill). Showing fake numbers while a real tournament is on is wrong. | Added `<BootGate>`: the root layout reads the server's `loading` flag and, during the boot window, renders a full-screen "Loading live tournament data…" screen instead of the sim. It polls `/api/live-status`, and the moment live data lands it `router.refresh()`es the content to live and reveals (safety-capped at 30s so it can never hang). Normal loads (live already in memory — the common case) render nothing and pay no cost. **Gotcha fixed in review:** the reveal `router.refresh()` flips the layout's `loading` prop, so the gate decision is captured once in a ref and the effect depends only on the stable `router` — otherwise the prop change cancelled the hide-timer and stranded the overlay. Verified: gate shows on boot, swaps to live, hides; no hydration errors. · BootGate.tsx · layout.tsx |
| WC-041 | A failed boot-time live fetch could **strand the app on the simulation** | instrumentation · loadTournament | The app boots on the deterministic simulation and loads live data in the background (so it serves instantly and never boots broken). But the initial `activateTournament('live-2026')` had **no retry**, and the 60s refresh loop's `refreshLiveScores()` no-ops while the active tournament is still `simulation` — so a single transient SportMonks hiccup at boot left the app stuck on the placeholder until the next deploy. (Compounded by a deploy storm: every redeploy reboots the instance into a fresh ~25s sim window.) | (1) Retry the initial load up to 3× with backoff before falling back. (2) Make the refresh loop **self-heal**: if the active tournament isn't `live-2026`, re-attempt the full load each cycle (flagging `loading` so the "Loading live data…" pill shows) instead of no-opping. A boot hiccup now recovers within ~60s instead of needing a redeploy. Verified the normal boot still swaps sim → live. · instrumentation.ts |
| WC-040 | **Home page blanked** (error boundary) — a narrative threw on a live data edge and took the whole route down | ai/narratives · home | Production home rendered the Next error boundary (digest, no content); `/api/insights` 500'd. A narrative generator (`generateInsights` / `generateDailyBriefing`) threw on a specific transient live snapshot (happened while between live matches; stopped reproducing once 6 went live — a data-edge case local prod mode with a fresh snapshot never hit). With no guard, the throw propagated to the route's error boundary and blanked everything. | Degrade, don't crash, per the app's own philosophy: `generateInsights`, `generateDailyBriefing` and `generateBriefingDeck` each build inside try/catch and return partial/neutral output (logging the error so the failing section shows in server logs); `homeData()` additionally wraps every panel in a `safe()` guard. Home now renders even if a narrative fails. **Follow-up:** exact throwing line not yet pinned (not reproducing + no log access at the time); the new `console.error`s will capture the stack if it recurs. Verified: home restored on production. · narratives.ts · queries.ts |
| WC-039 | Live ticker minute **sat frozen then jumped** — no clear cadence | home/LiveTicker | The ticker is server-rendered, so the minute only changed when a snapshot-generation bump triggered a `router.refresh()`, and the feed reports minutes sporadically. So `23′` sat still, then jumped to `26′` — read as "no methodology". | Split the minute into a tiny client clock (`LiveMinute`) that advances locally once a second from the snapshot's generation time and **re-anchors on every fresh snapshot**, so it ticks smoothly between the (still 15–45s) score updates. Freezes at `HT`, caps drift at +5 so a stalled refresh can't run it away, and seeds from the anchor so there's no hydration mismatch. Pure arithmetic extracted to `lib/matchClock.ts` (6 tests). Also surfaced HALFTIME games in the ticker instead of dropping them. · LiveTicker.tsx · LiveMinute.tsx · matchClock.ts |
| WC-038 | **Title favourite (Argentina) vanished** mid-group-stage — forecast showed 0% to even reach the R32 while still topping the power rankings | analytics/knockoutResults | The WC-033 reconciliation zeroed every team not in a real R32 tie. But SportMonks draws R32 ties **incrementally** as teams clinch, so ~4 R32 fixtures (8 teams) existed while 12 group games were still scheduled. The code read "an R32 tie exists" as "groups are over, this is the full qualifier list" and zeroed all 40 other teams — including Argentina (3-0, 2-0, a game in hand), France, Spain, England. Their `reachR32`/`winTitle` went to 0 while `powerRank` stayed 1, collapsing the whole title-odds board to a junk Brazil/Japan/Morocco/USA top-6. | Only treat the qualifier set as the **complete bracket** (and zero non-members) once **every group game is FINISHED** (`groupStageComplete`). Until then, still-qualifying sides keep their projection; teams already drawn into an R32 tie are still safely confirmed as through. Regression test covers the partial-R32-mid-group-stage case. · knockoutResults.ts |
| WC-037 | "How many goals has **Mbappé** scored" returned a **goals leaderboard**, ignoring the named player | ai/nlq | The metric leaderboard fired on the metric word alone, so a stat question about a specific player ("Messi xG", "how many assists does Bellingham have") showed the tournament leader instead of that player's value. | When a metric query also names a confident player (`bestPlayer`), route to a new `player-stat` intent — that player's value for the metric plus their rank ("Kylian Mbappé has 4 Goals — #2 of 871"); leaderboards still fire when no player is named. 1 data-driven test. · nlq.ts |
| WC-036 | Bare star-player names (**"Pedri", "Lionel Messi"**) returned the generic fallback from "Ask the data" | ai/query/resolver · ai/nlq | The NLQ lookup required `extractPlayers()` to return *exactly one* player, but that resolver includes a fuzzy (edit-distance ≤1) tier for the search dropdown — so "Pedri" matched Pedri **plus every "Pedro"** (1 edit away), giving ≥2 results and failing the `=== 1` gate → `unknown`. Names with no near-spellings ("Mbappe") happened to work, which masked it. | Added `bestPlayer()` to the resolver: the single most-confident match that **ignores the fuzzy tier** (matches only on a full name or an exact name token, ties broken by minutes), returning null when nothing confident matches. The NLQ lookup now uses it. Bare names resolve; typos/gibberish ("Pedrooo") still fall through to the helpful fallback rather than a wrong guess. 1 data-driven test. · resolver.ts · nlq.ts |
| WC-035 | "Ask the data" **mis-routed** many natural questions — a question that merely mentioned a team/player returned that entity's page (confident-but-wrong), and common asks (cards, standings, fixtures, coach, age) hit the generic fallback | ai/nlq | The deterministic parser ended on a greedy entity-lookup fallback: any query containing a team/player name returned its profile, so "what are the odds for Brazil", "is Spain better than France", "who is Argentina's coach" all returned a wrong-looking lookup. Whole common categories had no intent at all. | Added six data-backed intents (group standings, fixtures/next match, coach lookup, team comparison, youngest/oldest/tallest/shortest, discipline+clean-sheet+saves leaderboards), routed "odds/chances to win" to title odds, and added an **unsupported-topic guard** (injuries/transfers/tickets → honest "I don't track that"). Crucially, **gated the entity lookup** on a residual-word check so a question that only mentions an entity falls through to the helpful fallback instead of a wrong page. 5 new tests. (Bare-name resolver recall — e.g. "Pedri" — is a separate pre-existing `extractPlayers` gap, not changed here.) · nlq.ts |
| WC-034 | "Ask the data" returned the generic **"couldn't map that"** for tactics/coaching/style questions | ai/nlq · server/tactics | The NLQ had no intent for playing-style queries, so "best coaching tactics", "which teams press the highest", "Spain's playing style" all fell through to the unknown-intent fallback — even though per-team tactical profiles (possession, press index, field tilt, formation, style label) were already computed in `server/tactics.ts` and shown on team/match pages. | Added a `tactics` intent: a named team → its tactical identity (style label + blurb + metric bars + coach + most-used shape); otherwise a tactical-styles board across the field, sortable by press or possession from the query wording. New `tacticalBoard()` exposes the per-team numbers honestly (null where the source lacks team stats). Surfaced two example chips on /ask; 2 unit tests. · nlq.ts · tactics.ts |
| WC-033 | Forecast **ignored real knockout results** — an eliminated team could still show a "reach the semis" probability | analytics/engine · knockout reconcile | `runSimulation` projects the whole knockout from a seeded bracket every run, with no knowledge of who actually qualified or what's been played. Latent today (knockout fixtures are TBD placeholders), but the moment real ties resolve, a team already knocked out would still carry non-zero reach probabilities and a confirmed qualifier could read < 100% to make the R32. | Added `reconcileForecastsWithResults()` — a post-simulation pass that pins what's *known* from the real fixtures per team: not-qualified → all reach 0; appears at round R → reached R = 1; won at R → reached R+1; lost at R → nothing beyond R; won the final → title = 1. Still-open rounds keep the model's simulated odds. Penalty shootouts break level ties. No-op until real knockout fixtures exist, so the pre-tournament forecast is untouched (verified: current 48-team forecast unchanged, Argentina 25% etc.). Unit-tested (7 scenarios). **Follow-up (deferred, ROADMAP):** a full real-bracket *re-simulation* (propagating still-alive teams through the actual draw) needs FIFA's bracket tree reconstructed from SportMonks' "Winner Match N" linkage — fragile + unverifiable until the draw lands (2 Jul). · knockoutResults.ts · analytics/index.ts |
| WC-032 | SportMonks adapter **hardcoded `stage: 'GROUP'`** on every fixture | sportmonks adapter | Every fixture was stamped `stage: 'GROUP'`. Harmless today (knockout fixtures are TBD placeholders, filtered out by the participant-name check), but the moment real R32+ fixtures get their teams assigned (early July) they'd appear mislabelled as group games. Worse, `groupId` fell back to the **home team's group** when `f.group` was absent, so a knockout tie would have been counted into that group's standings (which filter by `groupId`). | Add the `stage` include and map `f.stage.name` → `MatchStage` via `mapStage()` (verified against all 7 real WC2026 stage strings: Group Stage / Round of 32 / Round of 16 / Quarter-finals / Semi-finals / 3rd Place Final / Final; specific rounds tested before the bare-"final" fallback). Set `groupId` only for group fixtures (null for knockouts) so a KO tie can never pollute a group table. Unit-tested (`sportmonks.test.ts`). · sportmonks.ts |
| WC-031 | AI Insights "Daily Briefing" showed a **frozen "13 June 2026 · Matchday 3"**, and every group match was internally tagged **matchday 1** | insights page · sportmonks adapter · narratives | Two bugs: (1) the briefing sub-heading was a **hardcoded literal** `"13 June 2026 · Matchday 3"` that never changed; (2) the SportMonks adapter **hardcoded `matchday: 1`** on every fixture even though it already fetched the `round` include — SportMonks carries the group-stage matchday in `round.name` ("1".."3") — so the real matchday was lost everywhere (briefing, `phaseLabel`, any matchday grouping). | Map `matchday` from `f.round?.name` in the adapter (numeric → matchday, else 1). Replace the hardcoded sub-heading with a data-driven `briefingMeta()` query: today's date + the current phase derived from the fixtures (live match → next scheduled → last finished; "Knockout stage" once the group games are all done). Only the live edition gets a calendar date; historical editions show just the phase. · sportmonks.ts · server/queries.ts · insights/page.tsx |
| WC-030 | Power rankings looked **different on the home panel vs the `/rankings` page**, and the top of the table reshuffled on its own | analytics/power · home · rankings | Both surfaces read the same `engine().powerRankings`, so they match within a single engine generation — but the live engine rebuilds (fresh Monte Carlo) whenever a match flips status (kickoff / final whistle), so a home panel rendered earlier and a freshly-navigated `/rankings` page can reflect two different simulations (ENH-1 class: no in-browser auto-refresh). Worse, several elite teams **saturate the 0..100 display ceiling** (Spain/France/Argentina all = 100), and the sort broke those ties by **array order**, which isn't stable across rebuilds — so the very top reordered on every status flip even when nothing changed. | Sort by the **un-clamped raw score** with **ELO** then teamId as deterministic final tie-breaks, so the order is identical on every rebuild and every page that reads it (`powerRating` still displays clamped 0..100). The remaining cross-time drift is genuine live movement, addressed by ENH-1 (browser auto-refresh). · power.ts |
| WC-029 | Golden Boot race **ranked a player with fewer goals above the actual leader** | analytics/goldenboot · home · predictions | The race was sorted by `projectedGoals` (current + finishing-adjusted rate × expected remaining games), not goals scored — so a deeper-running or hot-rate player could sit above the genuine top scorer, which reads as wrong for a "Golden Boot race." Compounding it, the per-90 rate divided by **actual minutes** (`/ max(mins,1)`), so a low-minute burst (a sub who scored in 30') projected a wild rate and inflated their finish. | Rank the race by **goals actually scored** (the real standings); projection + win-boot % ride alongside as forecast **columns**, not the sort key (tie-break: projection → xG → win prob). Also regress the rate denominator to a ~2-game sample (`/ max(mins,180)`) so a one-off burst can't leapfrog established scorers. Relabelled home panel "Projected finish" → "Goals → projected" and predictions subtitle → "Live standings + finishing-adjusted projection". · goldenboot.ts · page.tsx · predictions/page.tsx |

---

## Incidents (environment / config, not code defects)

| ID | Incident | Cause | Resolution |
|----|----------|-------|-----------|
| INC-1 | Live feed dead / hollow for hours | The key in Render was a **RapidAPI** key with no subscription, used against the **direct** api-sports.io host; also the free tier's 100 req/day is below one full load (~114 calls) | Use the correct direct `api-sports.io` key on a paid plan; `DATA_SOURCE=apifootball` |
| INC-2 | Deploys reported "Live" but ran old code | A Render **Rollback** pinned an old commit; env-var saves and "Clear build cache & deploy" rebuilt the *pinned* commit, not `HEAD` | Push a **fresh commit** — auto-deploy-on-commit always builds `HEAD` and breaks the pin |
| INC-3 | Prod reverted to old (pre-SportMonks / API-Football) code after env-var save AND again after a plan change (Free→Pro) | Same root cause as INC-2: a still-active **rollback pin** means **any service config change** (env save, instance-plan change, restart) redeploys the *pinned* old commit, not `HEAD`. Diagnosed via `/api/health` reporting `API-Football (live)` instead of `SportMonks (live)`. | Push a fresh commit to force `HEAD`. **Durable fix:** clear the rollback in Render → service → **Manual Deploy → Deploy latest commit**, so future config changes rebuild `HEAD`, not the pin. |

---

## Recurring patterns → where to focus testing

These are the classes of bug this codebase is prone to. Test these deliberately:

1. **Undefined entity lookups (the #1 class).** Any `getTeam`/`getPlayer`/`getPlayerView` with a `!`
   non-null assertion crashes when data is mid-load or a fixture's teams are TBD. **Test every page
   during the live-load window (first ~60s after a deploy) and with knockout (TBD) fixtures:**
   `/`, `/live`, `/matches`, `/matches/[id]`, `/standings`, `/rankings`, `/bracket`, `/insights`,
   `/storylines`, and search. Rule: never `!` a team/player lookup — resolve, check, skip.
2. **Match state handling (scheduled / live / halftime / finished).** Any label or narrative that
   assumes a match is over. Test a fixture in each of the four states.
3. **Timezone.** Any date/time display. Test from a non-UTC machine; check the day is correct, not
   just the time.
4. **Live refresh correctness.** Scores, minute, and status updating; standings/predictions
   recomputing after a goal; a match correctly flipping to FINISHED.
5. **Tournament switching.** Switching live-2026 ↔ men-2022 ↔ women-2023 etc. — indexes and the
   analytics engine must rebuild (same root cause as WC-008).
6. **Search forms.** Surname, full name, flipped order, abbreviated "F. Last", accents, prefix,
   typos, team codes/aliases. Covered by `src/ai/query/resolver.test.ts` but spot-check in the UI.
7. **Early-tournament / empty data.** 0 minutes played, 0 stats — per-90 and percentile math must
   not divide by zero or rank everyone identically.
8. **Hollow live feed.** If the API returns teams but no squads, the app must fall back to the full
   simulation (WC-005), not go blank.
9. **Match event timelines.** Goals, VAR/disallowed, cards, subs. Test three states: a **live** match
   (timeline grows each refresh), a **just-finished** match (final events captured), and an **older
   finished** match (timeline backfilled once). These have distinct code paths (WC-015).

---

## Known limitations (by design — not bugs)

- **Abbreviated player names** (`L. Messi`) come from the squad feed and upgrade to full names as
  players accrue match stats.
- **Live data depends on API-Football** coverage and the daily request quota.
- **SportMonks: market value** isn't provided (stays 0 — affects only the value-adjusted breakout
  weighting, which falls back to pure output). Ages, heights, full rosters, and managers are now
  fetched via per-team `players.player;coaches.coach` calls (resolved).
